package ch.epfl.ia.deliberative;

import logist.simulation.Vehicle;
import logist.task.TaskSet;
import logist.plan.Plan;

public class AStarPlanComputer implements PlanComputer {
    public Plan computePlan(Vehicle v, TaskSet ts) {
        Plan output = new Plan();

        return output;
    }
}
